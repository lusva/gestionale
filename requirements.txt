Django>=5.2,<5.3
django-htmx>=1.17
gunicorn>=23.0
Pillow>=10.0
python-stdnum>=1.20
# WeasyPrint per il rendering PDF dei documenti (gradient, custom
# properties, @page). Su macOS richiede `brew install pango`; manage.py
# setta DYLD_FALLBACK_LIBRARY_PATH se brew lib è presente. Su Debian/
# Ubuntu: `apt install libpango-1.0-0 libpangoft2-1.0-0`.
weasyprint>=68.0
# xhtml2pdf è ancora usato dall'app reports (export tabellare); il modulo
# documenti è migrato a WeasyPrint. reportlab pin: la 4.4 introduce un
# bug in tables.py che rompe xhtml2pdf 0.2.17.
xhtml2pdf>=0.2.16
reportlab<4.4
lxml>=5.2
pyHanko>=0.25
pyhanko-certvalidator>=0.26
